# Core (existing Flask app + PDF)
Flask>=3.0
PyMuPDF>=1.24
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4
joblib>=1.3
requests>=2.31

# FastAPI stack (Phase 8)
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Optional: spaCy improves education extraction (install model separately)
#   python -m spacy download en_core_web_sm
spacy>=3.7
